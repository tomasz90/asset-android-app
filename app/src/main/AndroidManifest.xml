<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.assets">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.AddAssetActivity" />
        <activity android:name=".activities.AddNobleMetalActivity" />
        <activity android:name=".activities.AddCurrencyActivity" />
        <activity android:name=".activities.AddCryptocurrencyActivity" />
        <activity android:name=".activities.AddStockActivity" />
        <activity android:name=".activities.AddETFActivity" />
        <activity android:name=".activities.AddRealEstateActivity" />
        <activity android:name=".asset_types.CryptoCurrencyType" />

    </application>

</manifest>